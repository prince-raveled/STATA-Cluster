# MicroVerse — SPEC section 20.
fastapi>=0.115
# Not uvicorn[standard]: that extra pins websockets==17.1, and the Vercel SDK needs
# websockets<17. Nothing here serves a WebSocket, and the extra's other additions --
# watchfiles, python-dotenv, PyYAML -- are unused too. uvloop is the one worth keeping
# and is named directly, because the container ran on the plain asyncio loop once
# before and nothing said so (see deploy/lock_requirements.py).
uvicorn>=0.30
uvloop>=0.15.1; sys_platform != "win32" and platform_python_implementation != "PyPy"
jinja2>=3.1
python-multipart>=0.0.9
sqlalchemy>=2.0
# PostgreSQL driver, for hosts without a disk to keep SQLite on. `[binary]` ships a
# prebuilt libpq so the build needs no C toolchain or system Postgres. Only loaded
# when MICROVERSE_DB names a postgresql:// URL; SQLite remains the default.
psycopg[binary]>=3.2

numpy>=1.26
pandas>=2.1
scipy>=1.11
statsmodels>=0.14
joblib>=1.3
h5py>=3.10          # BIOM v2 (HDF5) reader

# Optional: enables PyDESeq2 (fork 5, method 7). Specifications needing it are
# pruned with a visible reason when it is absent.
pydeseq2>=0.4

# Optional: when importable, skbio's ancombc / multi_replace are preferred over the
# in-tree implementations. No wheel on Python 3.13+ (biom-format fails to build).
# scikit-bio>=0.7.1

# Vercel Blob + Queues. Only imported when MICROVERSE_STORAGE=blob or
# MICROVERSE_JOBS=queue, so a server with a disk never loads it.
vercel>=0.11.3
